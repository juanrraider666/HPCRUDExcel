{# Los "-" guiones que se ven entre las definicones de etiquetas {%- y -%} son para quitar espacios en blanco y minimizar el código generado #}
{%- macro icon(icon, class, size) -%}
    {% if icon %}<i class="fa fa-{{ icon }} {{ class }}{{ size ? ' fa-'~size : null }}"></i>{% endif %}
{%- endmacro -%}

{%- macro icon_edit(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('edit', class, size) }}
{%- endmacro -%}

{%- macro icon_add(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('plus', class, size) }}
{%- endmacro -%}

{%- macro icon_clear(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('refresh', class, size) }}
{%- endmacro -%}

{%- macro icon_search(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('search', class, size) }}
{%- endmacro -%}

{%- macro icon_remove(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('trash', class, size) }}
{%- endmacro -%}

{%- macro icon_show(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('search', class, size) }}
{%- endmacro -%}

{%- macro icon_back(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('reply', class, size) }}
{%- endmacro -%}

{%- macro icon_catalog_back(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('caret-left', class, size) }}
{%- endmacro -%}

{%- macro icon_save(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('save', class, size) }}
{%- endmacro -%}

{%- macro icon_sublist(class, size) -%}
    {%- from _self import icon_edit -%}
    {{ icon_edit(class) }}
{%- endmacro -%}

{%- macro icon_won(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('smile-o', class, size) }}
{%- endmacro -%}

{%- macro icon_inactivate(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('trash', class, size) }}
{%- endmacro -%}

{%- macro icon_lock(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('lock', class, size) }}
{%- endmacro -%}

{%- macro icon_unlock(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('unlock', class, size) }}
{%- endmacro -%}

{%- macro icon_redeem(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('shopping-cart', class, size) }}
{%- endmacro -%}

{%- macro icon_times(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('times', class, size) }}
{%- endmacro -%}

{%- macro icon_check(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('check', class, size) }}
{%- endmacro -%}

{%- macro icon_uncheck(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('times', class, size) }}
{%- endmacro -%}

{%- macro icon_impersonate(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('user', class, size) }}
{%- endmacro -%}

{%- macro icon_configure(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('cog', class, size) }}
{%- endmacro -%}

{%- macro icon_edit_menu(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('ellipsis-v', class, size) }}
{%- endmacro -%}

{%- macro boolean_icon(value, class, size) -%}
    {%- from _self import icon_check, icon_uncheck -%}
    {{ value ? icon_check(class, size) : icon_uncheck(class, size) }}
{%- endmacro -%}

{%- macro icon_show_more(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('plus', class, size) }}
{%- endmacro -%}

{%- macro icon_detail(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('search', class, size) }}
{%- endmacro -%}

{%- macro icon_help(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('question', class, size) }}
{%- endmacro -%}

{%- macro icon_error_page(class, size = '6x') -%}
    {%- from _self import icon -%}
    {{ icon('exclamation-triangle', class, size) }}
{%- endmacro -%}

{%- macro icon_div_open(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('angle-right', class, size) }}
{%- endmacro -%}

{%- macro icon_div_close(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('angle-up', class, size) }}
{%- endmacro -%}

{%- macro icon_caret_right_link(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('caret-right', class, size) }}
{%- endmacro -%}

{%- macro icon_wishlist_add(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('star', class, size) }}
{%- endmacro -%}

{%- macro icon_email(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('envelope-o', class, size) }}
{%- endmacro -%}

{%- macro icon_user(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('users', class, size) }}
{%- endmacro -%}

{%- macro icon_approve(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('thumbs-o-up', class, size) }}
{%- endmacro -%}

{%- macro icon_reject(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('thumbs-o-down', class, size) }}
{%- endmacro -%}

{%- macro icon_loss(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('frown-o', class, size) }}
{%- endmacro -%}

{%- macro icon_canceled(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('times', class, size) }}
{%- endmacro -%}

{%- macro icon_list(class, size) -%}
    {%- from _self import icon -%}
    {{ icon('list', class, size) }}
{%- endmacro -%}